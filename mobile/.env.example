# API base URL — use your PC's LAN IP on a physical device (see Expo terminal: exp://192.168.x.x:8081)
# Emulator (Android): leave localhost; app auto-uses http://10.0.2.2:3000/api
# Physical phone: set both below to the same IP (no http:// prefix on DEV_HOST)
EXPO_PUBLIC_DEV_HOST=192.168.0.106
EXPO_PUBLIC_API_URL=http://192.168.0.106:3000/api
EXPO_PUBLIC_SENTRY_DSN=
